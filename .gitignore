# Ignore the contents of the _site directory and other cache directories
_site/
.sass-cache/

# Ignore the directory for local files during development
local/

# Ignore the Gemfile that is generated
Gemfile.lock

# Ingore files created by npm
node_modules
package-lock.json
# rendered at deploy time by .github/workflows/deploy.yml (and locally by .github/scripts/cv-pdf.js); never committed
files/yu-han-wu-cv.pdf
